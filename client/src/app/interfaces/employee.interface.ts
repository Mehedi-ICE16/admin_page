export interface IEmployee {
    id: string;
    email: string;
    password?:string;
    employee_id?:string
    // name: string;
    // phone: string;
    // active: boolean;
    // admin: boolean;
}